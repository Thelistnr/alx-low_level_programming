#include "main.h"
/**
 * _puts - Print a string followed by new line to std output
 * @str: Char
 * Return: 0
 */
void _puts(char *str)
{
	int a = 0;

	while (str[a] != '\0';
	{
		_putchar(str[a]);
		a++;
	}
	_putchar('\n');
}
